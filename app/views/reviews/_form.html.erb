<%= form_for([@product, @product.reviews.build]) do |f| %>


  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new review...", class: 'form-control' %>
  </div>

    <%= f.submit "Create Review", class:'btn btn-primary'  %>

<% end %>
