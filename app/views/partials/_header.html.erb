<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if logged_in? %>
      <%= link_to "Redmart App", products_path, class: 'navbar-brand' %>
        <% else %>
      <%= link_to "Redmart App", root_path, class: 'navbar-brand' %>
    <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">

        <% if logged_in? %>
          <li>
              <%= link_to "Logout", logout_path, method: :delete %>
          </li>
          <li>
          <%= link_to "Profile", user_path(current_user) %>
        </li>
        <% else %>
        <li>
          <%= link_to "Signup", signup_path %>
        </li>
        <li><%= link_to "Log in", login_path %></li>

        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Products <b class="caret"></b></a>
          <ul class="dropdown-menu">
             <% if current_user.admin? %>
            <li><%= link_to "Add Products", new_product_path %></li>
            <% end %>
              <li><%= link_to "View Products", products_path %></li>
          </ul></li>
          <% if current_user.admin? %>
          <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Users <b class="caret"></b></a>

            <ul class="dropdown-menu">
            <li><%= link_to 'Add Users', signup_path %></li>
             <li><%= link_to "View Users", users_path %></li>

           </ul>
         </li>
          <% end %>
          <% end %>
      </ul>

    </div><!--/.nav-collapse -->

  </div>
</nav>
